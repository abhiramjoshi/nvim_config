## NeoVim Config

The following is my nvim config using Lazy, Mason, etc. 

To add a language server, add it to `plugins/lsp-config.lua`

To change colorschemes, change the scheme in `plugins/colourscheme.lua`

In general, you can add plugins by adding them in the `plugins` directory
